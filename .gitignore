__pycache__/
*.pyc
~$*
.DS_Store

# transient live-capture buffer — overwritten every recording session, ~30 MB
data/study/_live_stream.tsv

# persisted live-inference model — ~50 MB, regenerate with train_live_model.py
model_store/